use apca::Client;

//make buy order given target allocation
pub fn buy_order(
    client: &Client,
    ticker: &str,
    target_weighting: &f32
)
{}

pub fn sell_order(
    client: &Client,
    ticker: &str,
    target_weighting: &f32
)
{}